import Button from '@Components/Button';
import './style.sass';

const HomePage = () => {
    return (
        <div>
            <Button
                label='Home'
                onClick={() => {}}
            />
        </div>
    )
}

export default HomePage;